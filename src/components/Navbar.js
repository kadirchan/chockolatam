import ConnectButton from "./ConnectButton";

const Navbar = () => {
    return (
        <div>
            <h1>Navbar</h1>
            <ConnectButton />
        </div>
    );
};

export default Navbar;
